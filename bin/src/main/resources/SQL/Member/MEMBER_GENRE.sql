CREATE TABLE MEMBER_GENRE (
	genreId NUMBER NOT NULL,
	memNo NUMBER NOT NULL
);

ALTER TABLE MEMBER_GENRE ADD CONSTRAINT PK_MEMBER_GENRE PRIMARY KEY (genreId, memNo);

ALTER TABLE MEMBER_GENRE ADD CONSTRAINT FK_MEMBER_GENRE_GENREID FOREIGN KEY (genreId) 
REFERENCES GENRE(genreId);

ALTER TABLE MEMBER_GENRE ADD CONSTRAINT FK_MEMBER_GENRE_MEMNO FOREIGN KEY (memNo) 
REFERENCES MEMBER(memNo);

COMMIT;